{
  "pluginInfo": {
    "name": "Archaeology Scanner Plugin",
    "version": "1.0",
    "description": "Scans the Archaeology Collector interface to read collection progress and update the UI accordingly."
  },
  "alt1Check": "if (!window.alt1) { alert(\"This app requires Alt1 to run!\"); }",
  "codeSections": {
    "collections": "const collections = { /* Add your collector/collection data structure here */ };",
    "scannerLogic": "async function scanCollections() { if (!alt1.overlayLockscreen()) return; try { const win = alt1.getActiveWindow(); if (!win || !win.title.includes(\"Archaeology Collector\")) { showError(\"Open collector interface first!\"); return; } const collectorName = alt1.getRegionText(win, [50, 80, 300, 30]); const collections = detectCollections(win); updateUI(collectorName, collections); } finally { alt1.overlayUnlockscreen(); } }",
    "detectCollections": "function detectCollections(win) { const results = []; const progressRects = [ [50, 120, 100, 30], [50, 160, 100, 30] /* add more positions as needed */ ]; for (const rect of progressRects) { const text = alt1.getRegionText(win, rect); const match = text.match(/(\\d+)\\/(\\d+)/); if (match) { results.push({ current: parseInt(match[1]), required: parseInt(match[2]) }); } } return results; }",
    "updateUI": "function updateUI(collectorName, collections) { const container = document.getElementById(\"collector-list\"); container.innerHTML = \"\"; const card = document.createElement(\"div\"); card.className = \"collector-card\"; const title = document.createElement(\"h2\"); title.className = \"collector-name\"; title.textContent = collectorName; card.appendChild(title); collections.forEach((col, index) => { const div = document.createElement(\"div\"); div.className = \"collection-item\"; const nameSpan = document.createElement(\"span\"); nameSpan.textContent = `Collection ${index + 1}`; const progressSpan = document.createElement(\"span\"); progressSpan.className = \"progress-text\"; progressSpan.textContent = `${col.current}/${col.required}`; div.appendChild(nameSpan); div.appendChild(progressSpan); card.appendChild(div); }); container.appendChild(card); }",
    "eventListeners": "document.getElementById(\"scan-btn\").addEventListener(\"click\", scanCollections); alt1.events.on(\"alt1pressed\", scanCollections);",
    "initialSetup": "if (alt1.permissionPixel) { alt1.requestPermission(); }"
  },
  "notes": "This JSON configuration encapsulates the entire logic of your Alt1 plugin. Modify the collections structure as needed to add more collectors or collection positions. Make sure to follow Alt1 guidelines for importing JSON configurations."
}
